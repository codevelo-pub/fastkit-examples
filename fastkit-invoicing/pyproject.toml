[project]
name = "fastkit-invoicing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastkit-cli>=0.2.0",
    "fastkit-core[postgresql-async,redis]>=0.4.0",
]
